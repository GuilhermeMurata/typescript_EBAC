"use strict";
function calcularArea(base, altura) {
    return base * altura;
}
const calcularArea2 = (base, altura) => base * altura;
function somar(...numeros) {
    // numeros.reduce()
    console.log(numeros);
}
